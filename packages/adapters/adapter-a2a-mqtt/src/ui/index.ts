/**
 * UI entry for the a2a_mqtt adapter.
 *
 * Reserved for the create-config builder + any custom UI hooks the host
 * needs. Round 1A intentionally leaves this empty — the adapter is not yet
 * registered in `builtin-adapter-types.ts`, so no UI surface needs to bind
 * to it. Round 2 will populate this alongside the host-side registration.
 */

export {};
